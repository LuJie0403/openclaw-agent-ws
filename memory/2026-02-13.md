# 2026-02-13 Interaction Log

## Deployment: openclaw-expenses (Completed)
- **Status**: Full deployment successful.
- **Backend**:
  - **Environment**: Installed Miniconda to provide Python 3.9 (replacing system Python 3.6).
  - **Dependencies**: Added `pydantic-settings` to fix Pydantic v2 compatibility.
  - **Code Fixes**: Patched `backend/app/expenses/service.py` to add missing imports (`Dict`, `get_db_connection`).
  - **Service**: Running via Systemd user service `openclaw-backend`.
- **Frontend**:
  - **Toolchain**: Switched from `npm` to `pnpm` (installed globally on remote).
  - **Build**: Encountered `vue-tsc` type check errors; bypassed using direct `vite build`.
  - **Status**: Static files generated in `dist/`.
- **Access**:
  - Frontend: `http://120.27.250.73`
  - Backend API: `http://120.27.250.73:8000/docs`
